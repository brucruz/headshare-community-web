query getCommunityTagsData($slug: String!) {
  community(slug: $slug) {
    errors {
      field
      message
    }
    community {
      ...CommonCommunity
      tags (limit: 5){
        title
        slug
      }
      banner {
        url
      }
      avatar {
        url
      }
    }
  }
}
