import styled from 'styled-components';

export const StatusSmiley = styled.span`
  font-weight: 800;
  font-size: 72px;
  line-height: 110px;
  /* identical to box height, or 153% */

  text-align: center;
  letter-spacing: 0.2px;

  color: var(--headshare-coral);
`;

export const StatusConfirmationTitle = styled.h2`
  margin: 15px 0;
  text-align: center;

  color: var(--main-titles);
`;

export const StatusConfirmationText = styled.p`
  text-align: center;
  color: var(--gray-text);
  font-size: 14px;
  line-height: 19px;

  white-space: break-spaces;
`;
